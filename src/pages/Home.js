import { Link } from "react-router-dom";

const Home = () => {
  return (
    <div className="homepage">
      <section className="greeting">
        <h2 className="home-greeting">Welcome to Chordler</h2>
      </section>
    </div>
  );
};

export default Home;
